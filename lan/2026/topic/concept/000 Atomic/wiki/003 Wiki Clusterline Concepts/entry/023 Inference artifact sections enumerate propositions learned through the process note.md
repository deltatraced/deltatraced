---
context_type: entry
---

Parent: [lan/2026/topic/concept/000 Atomic/wiki/003 Wiki Clusterline Concepts/003 Wiki Clusterline Concepts](../003%20Wiki%20Clusterline%20Concepts.md)

Spawned by: [lan/2026/topic/concept/000 Atomic/wikiproc/003 Wiki Proc Clusterline Concepts/003 Wiki Proc Clusterline Concepts](../../../wikiproc/003%20Wiki%20Proc%20Clusterline%20Concepts/003%20Wiki%20Proc%20Clusterline%20Concepts.md)

Spawned in: [^spawn-entry-2fb6d0](../../../wikiproc/003%20Wiki%20Proc%20Clusterline%20Concepts/003%20Wiki%20Proc%20Clusterline%20Concepts.md#spawn-entry-2fb6d0)

---

# What?

`# Inference` is an [artifact section](005%20Process%20Notes%20may%20contain%20artifact%20sections%20in%20non%20journal%20headers.md) that records propositions and explanations about the underlying [process note](../concept/000%20Term%20Process%20Note.md) context that exist independently of any changes that note introduces or plans to introduce.

`# Inference` can capture facts distilled from investigations about the underlying activity or the tools. It comes with a few different variants:

* as a `# Resolution` for note types which are resolved by an explanation or fact such as notes of type `investigation` and `issue`.
  * Expectations for how `# Resolution` is satisfied can be put under `# Objective`. Example: Some investigations and issues can only be resolved by explanations that make use of a program that reproduces some behavior of interest.
* as a `# Preconditions` for enumerating facts (and assumptions the note declares) which affect the operations in the note.

There is also notes with a dedicated type `inference` where such content can reside to facilitate reuse. They can be linked by other notes in their associated inference section and variants.

In contrast to `# Decision` artifact sections,

[022 Decision artifact sections enumerate key facts about changes and commitments produced by the process note](022%20Decision%20artifact%20sections%20enumerate%20key%20facts%20about%20changes%20and%20commitments%20produced%20by%20the%20process%20note.md). So these can record post conditions, prescribe policy decisions, and so on.

In general, `# Inference`s should be descriptive or explanatory. `# Decision`s on the other hand can explain changes but also prescribe new policy decisions and then justifies them with an explanation.
